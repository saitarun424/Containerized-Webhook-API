version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - WEBHOOK_SECRET=testsecret
      - DATABASE_URL=sqlite:////data/app.db
    volumes:
      - data:/data
volumes:
  data: